name: micasense-georef
channels:
  - conda-forge
dependencies:
  - python>=3.10
  - numpy>=1.24
  - pyproj>=3.4
  - pillow>=9.0
  - tifffile>=2023.1.0
  - rasterio>=1.3  # only needed for GeoTIFF output (--out)
